// generated by diplomat-tool
import { CalculatorConfig } from "./CalculatorConfig.mjs"
import { FfiLocation } from "./FfiLocation.mjs"
import { ZmanimErrorCode } from "./ZmanimErrorCode.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const FfiZmanimCalculator_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.FfiZmanimCalculator_destroy(ptr);
});

export class FfiZmanimCalculator {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("FfiZmanimCalculator is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            FfiZmanimCalculator_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    static new_(location, year, month, day, config) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();


        const result = wasm.FfiZmanimCalculator_new(location.ffiValue, year, month, day, CalculatorConfig._fromSuppliedValue(diplomatRuntime.internalConstructor, config)._intoFFI(diplomatRuntime.FUNCTION_PARAM_ALLOC.alloc(CalculatorConfig._sizeBytes), functionCleanupArena, {}, false));

        try {
            return result === 0 ? null : new FfiZmanimCalculator(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

        }
    }

    /**
     * Calculates a zman preset by index, returning UTC epoch milliseconds.
     *
     * The index matches {@link ZmanPresetId} in the generated {@link crate::zman_preset} module.
     */
    calculateZmanByIndex(presetIndex) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 9, 8, true);


        const result = wasm.FfiZmanimCalculator_calculate_zman_by_index(diplomatReceive.buffer, this.ffiValue, presetIndex);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new ZmanimErrorCode(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('ZmanimErrorCode.' + cause.value, { cause });
            }
            return (new BigInt64Array(wasm.memory.buffer, diplomatReceive.buffer, 1))[0];
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}