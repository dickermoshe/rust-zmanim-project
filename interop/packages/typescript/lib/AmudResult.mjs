// generated by diplomat-tool
import { SideCode } from "./SideCode.mjs"
import { TractateCode } from "./TractateCode.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



export class AmudResult {
    #tractate;
    get tractate() {
        return this.#tractate;
    }
    set tractate(value){
        this.#tractate = value;
    }
    #page;
    get page() {
        return this.#page;
    }
    set page(value){
        this.#page = value;
    }
    #side;
    get side() {
        return this.#side;
    }
    set side(value){
        this.#side = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new AmudResult(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("AmudResult's constructor takes an object of AmudResult's fields.");
        }

        if ("tractate" in structObj) {
            this.#tractate = structObj.tractate;
        } else {
            throw new Error("Missing required field tractate.");
        }

        if ("page" in structObj) {
            this.#page = structObj.page;
        } else {
            throw new Error("Missing required field page.");
        }

        if ("side" in structObj) {
            this.#side = structObj.side;
        } else {
            throw new Error("Missing required field side.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 12;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof AmudResult) {
            return obj;
        }

        return AmudResult.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#tractate.ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#page, Uint16Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#side.ffiValue, Int32Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("AmudResult._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const tractateDeref = diplomatRuntime.enumDiscriminant(wasm, ptr);
        structObj.tractate = new TractateCode(diplomatRuntime.internalConstructor, tractateDeref);
        const pageDeref = (new Uint16Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.page = pageDeref;
        const sideDeref = diplomatRuntime.enumDiscriminant(wasm, ptr + 8);
        structObj.side = new SideCode(diplomatRuntime.internalConstructor, sideDeref);

        return new AmudResult(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}