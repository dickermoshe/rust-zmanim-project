// generated by diplomat-tool
import { HolidayCode } from "./HolidayCode.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * A holiday occurring on a date, with an optional sub-day (Chanukah, Omer).
 */
export class HolidayEntry {
    #code;
    get code() {
        return this.#code;
    }
    set code(value){
        this.#code = value;
    }
    #subDay;
    get subDay() {
        return this.#subDay;
    }
    set subDay(value){
        this.#subDay = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new HolidayEntry(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("HolidayEntry's constructor takes an object of HolidayEntry's fields.");
        }

        if ("code" in structObj) {
            this.#code = structObj.code;
        } else {
            throw new Error("Missing required field code.");
        }

        if ("subDay" in structObj) {
            this.#subDay = structObj.subDay;
        } else {
            throw new Error("Missing required field subDay.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 8;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof HolidayEntry) {
            return obj;
        }

        return HolidayEntry.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#code.ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#subDay, Uint8Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("HolidayEntry._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const codeDeref = diplomatRuntime.enumDiscriminant(wasm, ptr);
        structObj.code = new HolidayCode(diplomatRuntime.internalConstructor, codeDeref);
        const subDayDeref = (new Uint8Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.subDay = subDayDeref;

        return new HolidayEntry(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}